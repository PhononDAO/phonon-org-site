# -- Site Configuration --
# Refer to the theme docs for more details about each of these parameters.
# https://jpanther.github.io/congo/docs/getting-started/

theme = "congo"
defaultContentLanguage = "en"
baseURL = "https://www.phonon.org/"

enableRobotsTXT = true
paginate = 25
summaryLength = 0
publishDir = "docs"

[outputs]
  home = ["HTML", "RSS", "JSON"]
